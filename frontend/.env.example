# Backend API base URL — used in composables via import.meta.env.VITE_API_URL
# Points to nginx which proxies /api/ to Symfony PHP-FPM backend
VITE_API_URL=http://localhost:8080/api

# Vite dev server port — used in Dockerfile CMD and vite.config.ts
VITE_PORT=5173
