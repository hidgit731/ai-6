+ Получил промпт для разбивки на фичи и запросил у него изготовление промптов для speckit.specify

+ Инициализация backend-сервиса (Symfony 8, PHP 8.4). Создать Symfony skeleton-проект в директории `backend_monolith/`. Настроить слоистую структуру директорий: Domain (Entity, Repository interfaces, Trait), Application (Service, DTO/Request, DTO/Response), Infrastructure (Persistence/Repository), Presentation (HTTP — invokable controllers). Добавить PHP CS-Fixer с конфигом, PHPUnit с базовым тестом, .gitignore. Не включать Docker, не включать БД-миграции — только скелет проекта с пустой структурой и инструментами качества кода.

  - Обнаружил, что контроллеры в плане собираются наследоваться от AbstractController. Внёс это уточнение в rules Подумал, что задачи получаются очень здоровые для того, чтобы я мог их проревью

+ Инициализация frontend-сервиса (Vue 3 SPA). Создать проект в директории `frontend/` с помощью Vite. Подключить Vue Router и Pinia. Настроить структуру: `src/components/`, `src/pages/`, `src/stores/`, `src/composables/`, `src/router/`. Добавить базовый layout-компонент с адаптивной вёрсткой (mobile-first). Добавить .gitignore. Не включать Docker — только скелет фронтенд-проекта.

+ Docker Compose инфраструктура (dev + prod). Нужно два окружения: dev — для локальной разработки с горячей перезагрузкой кода и отладкой, prod — для деплоя с иммутабельными образами. Четыре сервиса: Nginx (единая точка входа, маршрутизирует `/api/*` на backend и `/` на frontend), PostgreSQL 18 (база данных), backend_monolith (PHP 8.4-FPM, Symfony 8), frontend (Vue 3 SPA, Vite 7, Node 20). Dev и prod конфигурации разделены: базовый `docker-compose.yaml` описывает dev-окружение, `docker-compose.prod.yaml` содержит только prod-отличия. Каждый сервис хранит свои Dockerfile в `deploy/dev/` и `deploy/prod/` внутри своей директории. Общая инфраструктура (nginx, postgres) — в корневом `deploy/`. `.env.example` в корне и в каждом сервисе содержит все переменные с описательными комментариями. `.editorconfig` унифицирует стиль форматирования файлов.

+ Ошибки после деплой

  - При выполнении docker compose up получил ошибку '\[frontend 2/3\] RUN addgroup -g 1000 appgroup && adduser -u 1000 -G appgroup -s /bin/sh -D appuser: 0.298 addgroup: gid '1000' in use'
  - Появилась другая ошибка сборки: 'target backend_monolith: failed to solve: process "/bin/sh -c apk add --no-cache libpq-dev icu-dev oniguruma-dev && docker-php-ext-install pdo_pgsql opcache intl mbstring && pecl install xdebug && docker-php-ext-enable xdebug" did not complete successfully: exit code: 1'
  - При запуске 'docker compose up' сервис бэкенда падает с ошибкой 'Script cache:clear returned with error code 1 ... Environment variable not found: "DEFAULT_URI"'

+ CRUD заметок с Markdown-редактором. Backend: сущность Note (id UUID, title, content в Markdown, created_at, updated_at), интерфейс NoteRepositoryInterface + Doctrine-реализация, сервис NoteService (create, update, delete, getById, list с пагинацией), Request/Response DTO, invokable контроллеры для 5 эндпоинтов (list, get, create, update, delete). Подключить NelmioApiDocBundle для OpenAPI-документации. Написать минимум 4 unit/integration теста. Frontend: страница списка заметок с пагинацией, страница просмотра заметки (рендер Markdown в HTML), страница создания/редактирования с Markdown-редактором и live preview (split-pane). API-вызовы через composable `useNotes`, состояние в Pinia store `notesStore`. Адаптивная вёрстка (mobile-first).

+ Папки (блокноты) для группировки заметок. Backend: сущность Folder (id UUID, name, parent_id nullable для иерархии, created_at, updated_at), связь Note → Folder (ManyToOne, nullable). Интерфейс FolderRepositoryInterface + реализация. FolderService (create, update, delete, getTree, move). Контроллеры CRUD для папок + эндпоинт получения дерева. Эндпоинт перемещения заметки в папку. Request/Response DTO. Тесты для FolderService. Frontend: компонент FolderTree в sidebar с рекурсивным отображением иерархии, выбор папки фильтрует список заметок, drag-and-drop или кнопка для перемещения заметки. Pinia store `foldersStore`, composable `useFolders`.

+ Теги для заметок. Backend: сущность Tag (id UUID, name уникальный, created_at), связь ManyToMany между Note и Tag. TagRepositoryInterface + реализация. TagService (create, delete, getAll, getCloud — с количеством заметок на тег). Обновить NoteService: добавление/удаление тегов при создании/обновлении заметки. Контроллеры: CRUD тегов, эндпоинт облака тегов. Фильтрация заметок по одному или нескольким тегам (query-параметр). Тесты. Frontend: компонент TagCloud (размер шрифта пропорционален количеству заметок), компонент TagInput с автокомплитом для формы заметки, фильтрация списка заметок по выбранным тегам. Pinia store `tagsStore`, composable `useTags`.

+ Избранное и корзина. Backend: добавить поле `is_favorite` (boolean) в Note. Добавить поле `deleted_at` (nullable datetime) для soft-delete. NoteService: методы toggleFavorite, softDelete, restore, emptyTrash, getFavorites (список избранных с пагинацией), getTrash (список удалённых с пагинацией). Symfony Command для автоочистки заметок старше 30 дней из корзины (для запуска по cron). Контроллеры: toggle favorite, soft delete, restore, trash list. Тесты для soft-delete и автоочистки. Frontend: иконка «звёздочка» на карточке заметки для toggle, вкладка/страница «Избранное» со списком. Страница «Корзина» со списком удалённых заметок, кнопки «Восстановить» и «Удалить навсегда». Обновить `notesStore` и `useNotes`.

+ Полнотекстовый поиск по заметкам. Backend: добавить поле `search_vector` (tsvector) в таблицу notes. Создать миграцию с GIN-индексом. Триггер PostgreSQL для автообновления tsvector при INSERT/UPDATE (конкатенация title + content, конфигурация 'russian' или 'simple'). SearchService с методом search(query, page, limit) — использует tsquery с ранжированием ts_rank. Контроллер поиска (GET /api/notes/search?q=...). Response DTO с подсветкой найденных фрагментов (ts_headline). Тесты. Frontend: компонент SearchBar в header с debounce-ввводом, страница результатов поиска с подсвеченными фрагментами, интеграция с существующей навигацией. Composable `useSearch`, обновить store при необходимости.

+ История версий заметок. Backend: сущность NoteVersion (id UUID, note_id FK, title, content, version_number integer, created_at). NoteVersionRepositoryInterface + реализация. Обновить NoteService: при каждом update заметки автоматически сохранять предыдущее состояние как NoteVersion. NoteVersionService (getVersions — список версий заметки, getVersion — конкретная версия, revert — откат заметки к выбранной версии, что создаёт новую версию). Контроллеры: GET /api/notes/{id}/versions, GET /api/notes/{id}/versions/{versionId}, POST /api/notes/{id}/versions/{versionId}/revert. Тесты. Frontend: кнопка «История» на странице заметки, боковая панель или модальное окно со списком версий (дата, номер версии), просмотр diff между текущей и выбранной версией, кнопка «Откатить». Composable `useNoteVersions`.

+ Связи между заметками (wiki-ссылки и граф). Backend: сущность NoteLink (id UUID, source_note_id FK, target_note_id FK, created_at). NoteLinkRepositoryInterface + реализация. NoteLinkService: extractAndSyncLinks(note) — парсинг содержимого заметки, поиск паттернов `[[Название заметки]]`, создание/удаление связей. Вызывать при create/update заметки. Эндпоинты: GET /api/notes/{id}/links (входящие и исходящие ссылки), GET /api/graph (все заметки как узлы и связи как рёбра для визуализации). Response DTO для графа (nodes + edges). Тесты парсинга wiki-ссылок. Frontend: при рендере Markdown заменять `[[Название]]` на кликабельные ссылки (router-link). Страница «Граф знаний» с визуализацией через D3.js (force-directed graph): узлы = заметки, рёбра = связи, клик по узлу = переход к заметке. Composable `useNoteLinks`, composable `useGraph`.

+ Экспорт, дашборд, seed-данные и README. Backend: ExportService — метод exportMarkdown(noteId) возвращает .md файл, метод exportPdf(noteId) генерирует PDF (через Dompdf или аналог — рендер Markdown в HTML, затем HTML→PDF). Контроллеры: GET /api/notes/{id}/export/markdown (скачивание .md), GET /api/notes/{id}/export/pdf (скачивание .pdf). DashboardService — метод getStats(): количество заметок, тегов, папок, активность по дням за последние 30 дней (группировка по дате). Контроллер: GET /api/dashboard. Symfony Fixtures (DoctrineFixturesBundle): 5 папок (включая вложенные), 30+ заметок с Markdown-содержимым, 15 тегов привязанных к заметкам, 10+ wiki-ссылок между заметками, история версий для 5 заметок. Тесты для ExportService и DashboardService. Frontend: кнопки «Скачать .md» и «Скачать PDF» на странице заметки. Страница «Дашборд»: карточки со статистикой (количество заметок/тегов/папок), график активности по дням (Chart.js или аналог). Composable `useExport`, composable `useDashboard`. Создать README.md в корне проекта с описанием проекта, инструкцией по запуску, стеком технологий, скриншотами (placeholder).

+ Весь контент (например, notes-list-page, note-view-page) должен быть на всю ширину app-layout__content. У каждой заметки на списке есть 2 кнопки удаления: одна внутри блока заметки (около избранного), вторая справа от заметки. Надо удалить дубликат - ту, что справа. На странице списка заметок облако тегов переместить на правую сторону, т.к. это не первостепенный контент, а важны в первую очередь сами заметки. Из хэдера удалить блок логотипа совсем. Поиск в хэдере поставить по центру. Кнопки "Граф" и "Дашборд" перенести в сайдбар, поместив их ниже блока с директориями и отделив линией. Они должны быть похожи на кнопки "Избранное" и "Корзина". Также нужно подобрать им иконки, как это сделано, например, для Корзины в сайдбаре. "Граф" переименовать в "Карта связей". В корзине нужна кнопка "Удалить все записи навсегда", которая должна хард-удалять все софт-удалённые заметки, чтобы не тыкать на каждую "Удалить навсегда".
